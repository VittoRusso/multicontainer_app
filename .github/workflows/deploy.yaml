name: Deploy MultiDocker
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t vittorusso/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true vittorusso/react-test npm test

      - run: docker build -t vittorusso/multi-client ./client
      - run: docker build -t vittorusso/multi-nginx ./nginx
      - run: docker build -t vittorusso/multi-server ./server
      - run: docker build -t vittorusso/multi-worker ./worker

      - run: docker push vittorusso/multi-client
      - run: docker push vittorusso/multi-nginx
      - run: docker push vittorusso/multi-server
      - run: docker push vittorusso/multi-worker
