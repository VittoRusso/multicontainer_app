name: Deploy MultiDocker
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t vittorusso/multicontainer_app/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true vittorusso/react-test npm test

      - run: docker build -t vittorusso/multicontainer_app/multi-client ./client
      - run: docker build -t vittorusso/multicontainer_app/multi-nginx ./nginx
      - run: docker build -t vittorusso/multicontainer_app/multi-server ./server
      - run: docker build -t vittorusso/multicontainer_app/multi-worker ./worker

      - run: docker push vittorusso/multicontainer_app/multi-client
      - run: docker push vittorusso/multicontainer_app/multi-nginx
      - run: docker push vittorusso/multicontainer_app/multi-server
      - run: docker push vittorusso/multicontainer_app/multi-worker
